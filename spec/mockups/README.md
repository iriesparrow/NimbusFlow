# Mockups — PLACEHOLDER

> ⚠️ The HTML design references are **not yet committed**. The maintainer must add
> `dashboard-v2.html` (the front-end reference named in `CLAUDE.md`) and any other mockups here
> **before step 3 (Read-only client view)**, which is required to "match the mockups."
>
> Step 1 (scaffold) does not depend on these.
